@model CourseVM
@{
    var stud = ViewBag.student;
}

          
        
    <div class="card-body p-4">
        <form asp-action="Add" method="post" class="row">
            <input name="StudentId" value="@stud" hidden />
            <div class="border p-3">
            <div class="col-12 text-center">
                <h2 class="text-dark py-2">Add Course</h2>
            </div>

                <select asp-for="course.Id" asp-items="@Model.CourseList" class="form-select ">
                    <option disabled selected>--Select Course--</option>
                </select>
                <label asp-for="course.Id" name="obj" class="ms-2"></label>
                <span asp-validation-for="course.Id" class="text-danger"></span>
            </div>



            <div class="row pt-2">
                <div class="col-6 col-md-3">
                    <button type="submit" class="btn btn-primary form-control">Create</button>
                </div>
                <div class="col-6 col-md-3">
                    <a asp-controller="Student" asp-action="Index" class="btn btn-outline-primary border form-control ">Back to List</a>
                </div>
            </div>
        </form>
    </div>

</div>
